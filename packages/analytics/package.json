{
  "name": "@ogcio/analytics",
  "version": "0.0.2",
  "description": "",
  "scripts": {
    "test": "vitest run --coverage --outputFile=results.xml",
    "test:local": "vitest run",
    "build": "npm run build:clean && npm run build:prepare",
    "build:clean": "rm -rf dist && rm -f *.tsbuildinfo",
    "build:prepare": "tsc"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@ogcio/building-blocks-sdk": "^0.1.12",
    "next": "^14",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/react": "^16.1.0",
    "@types/node": "22.10.2",
    "@types/react": "18.3.12",
    "@types/react-dom": "18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2"
  }
}
